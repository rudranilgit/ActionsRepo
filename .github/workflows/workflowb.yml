name: Workflow B
on :
    workflow_run:
        workflows: ["Workflow A"]
        types:
            - completed
jobs:
    job1:
        if: ${{ github.event.workflow_run.conclusion == 'success' }}
        runs-on: ubuntu-latest
        steps:
            - name: Greeting
              run: echo "Hello ${{ github.event.workflow_run.outputs.job1_output }}! "